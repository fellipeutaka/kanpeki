---
title: Popover
description: Displays rich content in a portal, triggered by a button.
links:
  docs: https://react-aria.adobe.com/Popover
---

<ComponentPreview name="popover-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/popover
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="popover" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="single">Single import</Tabs.Trigger>
  <Tabs.Trigger id="multiple">Multiple imports</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="single">

```tsx
import { Button } from "~/components/ui/button";
import { Popover } from "~/components/ui/popover";

<Popover.Root>
  <Button>...</Button>

  <Popover.Content>
    <Popover.Arrow />
    ...
  </Popover.Content>
</Popover.Root>;
```

</Tabs.Content>

<Tabs.Content id="multiple">

```tsx
import { Button } from "~/components/ui/button";
import {
  PopoverArrow,
  PopoverContent,
  PopoverRoot,
} from "~/components/ui/popover";

<PopoverRoot>
  <Button>...</Button>
  <PopoverContent>
    <PopoverArrow />
    ...
  </PopoverContent>
</PopoverRoot>;
```

</Tabs.Content>

</Tabs.Root>

## Examples

### With Arrow

<ComponentPreview name="popover-with-arrow-demo" />

## API Reference

### Popover.Root

<PropsTable
  data={[
    {
      name: "children",
      required: true,
      typeSimple: "ReactNode",
      description: "The trigger and popover content.",
    },
    {
      name: "isOpen",
      required: false,
      typeSimple: "boolean",
      description: "Whether the popover is open (controlled).",
    },
    {
      name: "defaultOpen",
      required: false,
      typeSimple: "boolean",
      description: "Whether the popover is open by default (uncontrolled).",
    },
    {
      name: "onOpenChange",
      required: false,
      typeSimple: "function",
      type: "(isOpen: boolean) => void",
      description: "Handler called when the open state changes.",
    },
  ]}
/>

### Popover.Content

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The content to display within the popover.",
    },
    {
      name: "placement",
      required: false,
      typeSimple: "Placement",
      default: '"bottom start"',
      description: "The placement of the popover relative to the trigger.",
    },
    {
      name: "offset",
      required: false,
      typeSimple: "number",
      default: "4",
      description: "The additional offset applied along the main axis between the popover and its trigger.",
    },
    {
      name: "crossOffset",
      required: false,
      typeSimple: "number",
      default: "0",
      description: "The additional offset applied along the cross axis between the popover and its trigger.",
    },
    {
      name: "containerPadding",
      required: false,
      typeSimple: "number",
      default: "12",
      description: "The placement padding that should be applied between the popover and its surrounding container.",
    },
    {
      name: "shouldFlip",
      required: false,
      typeSimple: "boolean",
      default: "true",
      description: "Whether the popover should flip when it doesn't fit in the viewport.",
    },
    {
      name: "isNonModal",
      required: false,
      typeSimple: "boolean",
      description: "Whether the popover is non-modal (does not block interaction with the rest of the page).",
    },
    {
      name: "isKeyboardDismissDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether pressing the escape key to close the popover is disabled.",
    },
  ]}
/>

### Popover.Arrow

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "Custom arrow element. Defaults to a triangle SVG.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the arrow.",
    },
  ]}
/>
