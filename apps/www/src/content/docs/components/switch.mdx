---
title: Switch
description: A control that allows the user to toggle between on and off.
links:
  docs: https://react-aria.adobe.com/Switch
---

<ComponentPreview name="switch-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/switch
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="switch" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="single">Single import</Tabs.Trigger>
  <Tabs.Trigger id="multiple">Multiple imports</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="single">

```tsx
import { Switch } from "~/components/ui/switch";

<Switch.Root>
  <Switch.Track>
    <Switch.Thumb />
  </Switch.Track>
</Switch.Root>;
```

</Tabs.Content>

<Tabs.Content id="multiple">

```tsx
import { SwitchRoot, SwitchTrack, SwitchThumb } from "~/components/ui/switch";

<SwitchRoot>
  <SwitchTrack>
    <SwitchThumb />
  </SwitchTrack>
</SwitchRoot>;
```

</Tabs.Content>

</Tabs.Root>

## Examples

### Group

<ComponentPreview name="switch-group-demo" />

### Form

<ComponentPreview name="switch-form" />

## API Reference

### Switch.Root

<PropsTable
  data={[
    {
      name: "isSelected",
      required: false,
      typeSimple: "boolean",
      description: "Whether the switch is selected (controlled).",
    },
    {
      name: "defaultSelected",
      required: false,
      typeSimple: "boolean",
      description: "Whether the switch is selected by default (uncontrolled).",
    },
    {
      name: "onChange",
      required: false,
      typeSimple: "function",
      type: "(isSelected: boolean) => void",
      description: "Handler called when the switch selection state changes.",
    },
    {
      name: "isDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether the switch is disabled.",
    },
    {
      name: "isReadOnly",
      required: false,
      typeSimple: "boolean",
      description: "Whether the switch is read only.",
    },
    {
      name: "name",
      required: false,
      typeSimple: "string",
      description: "The name of the switch, used when submitting an HTML form.",
    },
    {
      name: "value",
      required: false,
      typeSimple: "string",
      description: "The value of the switch, used when submitting an HTML form.",
    },
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      type: "ReactNode | (values: SwitchRenderProps) => ReactNode",
      description: "The children of the switch.",
    },
  ]}
/>

### Switch.Track

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the switch track.",
    },
  ]}
/>

### Switch.Thumb

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the switch thumb.",
    },
  ]}
/>
