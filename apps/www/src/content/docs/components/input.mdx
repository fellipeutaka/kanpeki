---
title: Input
description: Displays a form input field or a component that looks like an input field.
links:
  docs: https://react-aria.adobe.com/TextField#text-field
---

<ComponentPreview name="input-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/input
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="input" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

```tsx
import { Input } from "~/components/ui/input";

<Input />;
```

## Examples

### File

<ComponentPreview name="input-file-demo" />

<Alert.Root>
  <AlertIcon variant="info" />
  <Alert.Description>
    For more customizable file upload components, see [File
    Trigger](/docs/components/file-trigger) or
    [Dropzone](/docs/components/dropzone).
  </Alert.Description>
</Alert.Root>

### Disabled

<ComponentPreview name="input-disabled-demo" />

### With Label

<ComponentPreview name="input-with-label-demo" />

<Alert.Root>
  <AlertIcon variant="info" />
  <Alert.Description>
    For better form integration with labels, descriptions, and error messages,
    use [Field](/docs/components/field#input) component with TextField.
  </Alert.Description>
</Alert.Root>

### With Button

<ComponentPreview name="input-with-button-demo" />

## API Reference

### Input

<PropsTable
  data={[
    {
      name: "type",
      required: false,
      typeSimple: "string",
      default: '"text"',
      description: "The input type (text, email, password, number, etc.).",
    },
    {
      name: "placeholder",
      required: false,
      typeSimple: "string",
      description: "Temporary text that occupies the input when it is empty.",
    },
    {
      name: "disabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether the input is disabled.",
    },
    {
      name: "readOnly",
      required: false,
      typeSimple: "boolean",
      description: "Whether the input is read only.",
    },
    {
      name: "required",
      required: false,
      typeSimple: "boolean",
      description: "Whether the input is required.",
    },
    {
      name: "minLength",
      required: false,
      typeSimple: "number",
      description: "The minimum number of characters supported by the input.",
    },
    {
      name: "maxLength",
      required: false,
      typeSimple: "number",
      description: "The maximum number of characters supported by the input.",
    },
    {
      name: "pattern",
      required: false,
      typeSimple: "string",
      description: "A regular expression the input's value must match.",
    },
    {
      name: "autoComplete",
      required: false,
      typeSimple: "string",
      description: "Describes the type of autocomplete functionality the input should provide.",
    },
    {
      name: "name",
      required: false,
      typeSimple: "string",
      description: "The name of the input, used when submitting an HTML form.",
    },
    {
      name: "autoFocus",
      required: false,
      typeSimple: "boolean",
      description: "Whether the input should receive focus on render.",
    },
  ]}
/>
