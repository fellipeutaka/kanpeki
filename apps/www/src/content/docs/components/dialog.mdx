---
title: Dialog
description: A window overlaid on either the primary window or another dialog window, rendering the content underneath inert.
links:
  docs: https://react-aria.adobe.com/Modal
---

<ComponentPreview name="dialog-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/dialog
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="dialog" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="single">Single import</Tabs.Trigger>
  <Tabs.Trigger id="multiple">Multiple imports</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="single">

```tsx
import { Button } from "~/components/ui/button";
import { Dialog } from "~/components/ui/dialog";

<Dialog.Root>
  <Button>Trigger</Button>

  <Dialog.Overlay>
    <Dialog.Modal>
      <Dialog.Content>
        <Dialog.Header>
          <Dialog.Title />
          <Dialog.Description />
        </Dialog.Header>

        <Dialog.Footer>
          <Button slot="close" />
        </Dialog.Footer>

        <Dialog.Close />
      </Dialog.Content>
    </Dialog.Modal>
  </Dialog.Overlay>
</Dialog.Root>;
```

</Tabs.Content>

<Tabs.Content id="multiple">

```tsx
import { Button } from "~/components/ui/button";
import {
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogModal,
  DialogOverlay,
  DialogRoot,
  DialogTitle,
} from "~/components/ui/dialog";

<DialogRoot>
  <Button>Trigger</Button>

  <DialogOverlay>
    <DialogModal>
      <DialogContent>
        <DialogHeader>
          <DialogTitle />
          <DialogDescription />
        </DialogHeader>

        <DialogFooter>
          <Button slot="close" />
        </DialogFooter>

        <DialogClose />
      </DialogContent>
    </DialogModal>
  </DialogOverlay>
</DialogRoot>;
```

</Tabs.Content>

</Tabs.Root>

## Examples

### Scrollable

<ComponentPreview name="dialog-scrollable-demo" />

### Alert Dialog

Use the `role="alertdialog"{:tsx}` prop on the `<Dialog.Content>{:tsx}` element to make an alert dialog.
Also, make sure to set the `isDismissable` prop to `false{:ts}` on the `<Dialog.Overlay>{:tsx}` or `<Dialog.Modal>{:tsx}`
to prevent the dialog from being dismissed by clicking outside of it.

<ComponentPreview name="alert-dialog-demo" />

### Sheet

Use the `side{:tsx}` prop on the `<Dialog.Modal>{:tsx}` element to make a sheet dialog.

<ComponentPreview name="dialog-sheet-demo" />

## API Reference

### Dialog.Root

<PropsTable
  data={[
    {
      name: "children",
      required: true,
      typeSimple: "ReactNode",
      description: "The trigger and dialog content.",
    },
    {
      name: "isOpen",
      required: false,
      typeSimple: "boolean",
      description: "Whether the dialog is open (controlled).",
    },
    {
      name: "defaultOpen",
      required: false,
      typeSimple: "boolean",
      description: "Whether the dialog is open by default (uncontrolled).",
    },
    {
      name: "onOpenChange",
      required: false,
      typeSimple: "function",
      type: "(isOpen: boolean) => void",
      description: "Handler called when the open state changes.",
    },
  ]}
/>

### Dialog.Overlay

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The modal content.",
    },
    {
      name: "isBlurred",
      required: false,
      typeSimple: "boolean",
      default: "false",
      description: "Whether the content behind the overlay should be blurred.",
    },
    {
      name: "isDismissable",
      required: false,
      typeSimple: "boolean",
      default: "true",
      description: "Whether the dialog can be dismissed by clicking the overlay.",
    },
    {
      name: "isKeyboardDismissDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether pressing the escape key to close the dialog is disabled.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the overlay.",
    },
  ]}
/>

### Dialog.Modal

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The dialog content.",
    },
    {
      name: "isDismissable",
      required: false,
      typeSimple: "boolean",
      default: "true",
      description: "Whether the dialog can be dismissed by clicking outside.",
    },
    {
      name: "isKeyboardDismissDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether pressing the escape key to close the dialog is disabled.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the modal.",
    },
  ]}
/>

### Dialog.Content

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The content of the dialog.",
    },
    {
      name: "role",
      required: false,
      typeSimple: "enum",
      type: '"dialog" | "alertdialog"',
      default: '"dialog"',
      description: "The role of the dialog.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the content.",
    },
  ]}
/>

### Dialog.Header

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The header content (title, description).",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the header.",
    },
  ]}
/>

### Dialog.Title

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The title text.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the title.",
    },
  ]}
/>

### Dialog.Description

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The description text.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the description.",
    },
  ]}
/>

### Dialog.Footer

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description: "The footer content (actions, buttons).",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the footer.",
    },
  ]}
/>

### Dialog.Close

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the close button.",
    },
  ]}
/>
