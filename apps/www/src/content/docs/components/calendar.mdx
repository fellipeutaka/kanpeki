---
title: Calendar
description: A calendar displays one or more date grids and allows users to select a single date.
links:
  docs: https://react-aria.adobe.com/Calendar
---

<ComponentPreview name="calendar-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/calendar
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @internationalized/date
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="calendar" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="single">Single import</Tabs.Trigger>
  <Tabs.Trigger id="multiple">Multiple imports</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="single">

```tsx
import { Button } from "~/components/ui/button";
import { Calendar } from "~/components/ui/calendar";

<Calendar.Root>
  <Calendar.Header>
    <Calendar.Nav>
      <Button slot="previous"></Button>
      <Calendar.Month />
      <Button slot="next"></Button>
    </Calendar.Nav>
  </Calendar.Header>

  <Calendar.Grid>
    <Calendar.GridHeader>
      {(weekDay) => <Calendar.HeaderCell>{weekDay}</Calendar.HeaderCell>}
    </Calendar.GridHeader>
    <Calendar.GridBody>
      {(date) => <Calendar.Cell date={date} />}
    </Calendar.GridBody>
  </Calendar.Grid>
</Calendar.Root>;
```

</Tabs.Content>

<Tabs.Content id="multiple">

```tsx
import { Button } from "~/components/ui/button";
import {
  CalendarCell,
  CalendarGrid,
  CalendarGridBody,
  CalendarGridHeader,
  CalendarHeader,
  CalendarHeaderCell,
  CalendarMonth,
  CalendarNav,
  CalendarRoot,
} from "~/components/ui/calendar";

<CalendarRoot>
  <CalendarHeader>
    <CalendarNav>
      <Button slot="previous"></Button>
      <CalendarMonth />
      <Button slot="next"></Button>
    </CalendarNav>
  </CalendarHeader>

  <CalendarGrid>
    <CalendarGridHeader>
      {(weekDay) => <CalendarHeaderCell>{weekDay}</CalendarHeaderCell>}
    </CalendarGridHeader>
    <CalendarGridBody>
      {(date) => <CalendarCell date={date} />}
    </CalendarGridBody>
  </CalendarGrid>
</CalendarRoot>;
```

</Tabs.Content>

</Tabs.Root>

## Examples

### Range

<ComponentPreview name="calendar-range-demo" />

## API Reference

### Calendar.Root

<PropsTable
  data={[
    {
      name: "value",
      required: false,
      typeSimple: "DateValue",
      description: "The current value (controlled).",
    },
    {
      name: "defaultValue",
      required: false,
      typeSimple: "DateValue",
      description: "The default value (uncontrolled).",
    },
    {
      name: "onChange",
      required: false,
      typeSimple: "function",
      type: "(value: DateValue) => void",
      description: "Handler called when the value changes.",
    },
    {
      name: "minValue",
      required: false,
      typeSimple: "DateValue",
      description: "The minimum allowed date.",
    },
    {
      name: "maxValue",
      required: false,
      typeSimple: "DateValue",
      description: "The maximum allowed date.",
    },
    {
      name: "isDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether the calendar is disabled.",
    },
    {
      name: "isReadOnly",
      required: false,
      typeSimple: "boolean",
      description: "Whether the calendar is read only.",
    },
    {
      name: "isDateUnavailable",
      required: false,
      typeSimple: "function",
      type: "(date: DateValue) => boolean",
      description: "Callback to determine if a date is unavailable.",
    },
    {
      name: "variant",
      required: false,
      typeSimple: "enum",
      type: '"outline"',
      description: "The visual style of the calendar.",
    },
  ]}
/>

### Calendar.Header

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the header.",
    },
  ]}
/>

### Calendar.Nav

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the navigation container.",
    },
  ]}
/>

### Calendar.Month

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the month heading.",
    },
  ]}
/>

### Calendar.Grid

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the grid.",
    },
  ]}
/>

### Calendar.GridHeader

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "function",
      type: "(weekDay: string) => ReactNode",
      description: "Render function for week day headers.",
    },
  ]}
/>

### Calendar.HeaderCell

<PropsTable
  data={[
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the header cell.",
    },
  ]}
/>

### Calendar.GridBody

<PropsTable
  data={[
    {
      name: "children",
      required: false,
      typeSimple: "function",
      type: "(date: CalendarDate) => ReactNode",
      description: "Render function for date cells.",
    },
  ]}
/>

### Calendar.Cell

<PropsTable
  data={[
    {
      name: "date",
      required: true,
      typeSimple: "CalendarDate",
      description: "The date for this cell.",
    },
    {
      name: "shape",
      required: false,
      typeSimple: "enum",
      type: '"default" | "rounded"',
      default: '"default"',
      description: "The shape of the cell.",
    },
    {
      name: "className",
      required: false,
      typeSimple: "string",
      description: "Additional CSS classes for the cell.",
    },
  ]}
/>

### RangeCalendar.Root

Used for selecting date ranges. Accepts the same props as `Calendar.Root` but with range values.

<PropsTable
  data={[
    {
      name: "value",
      required: false,
      typeSimple: "RangeValue<DateValue>",
      description: "The current range value (controlled).",
    },
    {
      name: "defaultValue",
      required: false,
      typeSimple: "RangeValue<DateValue>",
      description: "The default range value (uncontrolled).",
    },
    {
      name: "onChange",
      required: false,
      typeSimple: "function",
      type: "(value: RangeValue<DateValue>) => void",
      description: "Handler called when the range value changes.",
    },
    {
      name: "minValue",
      required: false,
      typeSimple: "DateValue",
      description: "The minimum allowed date.",
    },
    {
      name: "maxValue",
      required: false,
      typeSimple: "DateValue",
      description: "The maximum allowed date.",
    },
    {
      name: "isDisabled",
      required: false,
      typeSimple: "boolean",
      description: "Whether the calendar is disabled.",
    },
    {
      name: "isReadOnly",
      required: false,
      typeSimple: "boolean",
      description: "Whether the calendar is read only.",
    },
    {
      name: "isDateUnavailable",
      required: false,
      typeSimple: "function",
      type: "(date: DateValue) => boolean",
      description: "Callback to determine if a date is unavailable.",
    },
    {
      name: "variant",
      required: false,
      typeSimple: "enum",
      type: '"outline"',
      description: "The visual style of the calendar.",
    },
  ]}
/>
