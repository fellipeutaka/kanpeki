---
title: Autocomplete
description: An autocomplete combines a TextField or SearchField with a Menu or ListBox, allowing users to search or filter a list of suggestions.
links:
  docs: https://react-aria.adobe.com/Autocomplete
---

<ComponentPreview name="autocomplete-demo" />

## Installation

<Tabs.Root>

<Tabs.List>
  <Tabs.Trigger id="cli">CLI</Tabs.Trigger>
  <Tabs.Trigger id="manual">Manual</Tabs.Trigger>
</Tabs.List>
<Tabs.Content id="cli">

```bash
npx shadcn@latest add @kanpeki/autocomplete
```

</Tabs.Content>

<Tabs.Content id="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="autocomplete" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</Tabs.Content>

</Tabs.Root>

## Anatomy

```tsx
import { Autocomplete } from "~/components/ui/autocomplete";
import { Menu } from "~/components/ui/menu";
import { SearchField } from "~/components/ui/search-field";

<Autocomplete>
  <SearchField /> or <TextField />
  <Menu /> or <ListBox />
</Autocomplete>;
```

## Examples

### Dialog

<ComponentPreview name="autocomplete-dialog-demo" />

## API Reference

<PropsTable
  data={[
    {
      name: "filter",
      required: false,
      typeSimple: "function",
      type: "(textValue: string, inputValue: string, node: Node<T>) => boolean",
      description:
        "Custom filter function to determine which items match the input. Defaults to case-insensitive contains matching.",
    },
    {
      name: "options",
      required: false,
      typeSimple: "Intl.CollatorOptions",
      description: "Options for the default filter's Intl.Collator.",
    },
    {
      name: "disableVirtualFocus",
      required: false,
      typeSimple: "boolean",
      default: "false",
      description:
        "Whether the autocomplete should disable virtual focus, instead making the wrapped collection directly tabbable.",
    },
    {
      name: "disableAutoFocusFirst",
      required: false,
      typeSimple: "boolean",
      default: "false",
      description:
        "Whether or not to focus the first item in the collection after a filter is performed. Note this is only applicable if virtual focus behavior is not turned off via disableVirtualFocus.",
    },
    {
      name: "defaultInputValue",
      required: false,
      typeSimple: "string",
      description: "The default value of the input (uncontrolled).",
    },
    {
      name: "inputValue",
      required: false,
      typeSimple: "string",
      description: "The current value of the input (controlled).",
    },
    {
      name: "onInputChange",
      required: false,
      typeSimple: "function",
      type: "(value: string) => void",
      description: "Handler called when the input value changes.",
    },
    {
      name: "children",
      required: false,
      typeSimple: "ReactNode",
      description:
        "The children of the autocomplete, typically a text field and a menu or listbox.",
    },
  ]}
/>
